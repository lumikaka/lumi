# Embedded Markdown must have identical bytes on every build platform.
*.md text eol=lf

# Frontend source assertions must see the same line endings on Windows and macOS.
*.js text eol=lf
*.jsx text eol=lf
*.sass text eol=lf
